# AI Engineering Log — StackSift

> Maintained by the StackSift team as part of the LIFE Fellows AI Engineering Capstone.
> Every significant AI-assisted session must be logged here.

| Date | Tool | Prompt Summary | Output Quality | Time Saved | Lessons Learned |
|------|------|---------------|---------------|------------|-----------------|
| 2026-03-26 | Antigravity (Gemini) | Initialize StackSift monorepo: directory structure, docker-compose, .cursorrules, README, pnpm workspace, init script | ⭐⭐⭐⭐⭐ Excellent | ~3 hours | Prompt specificity is critical — listing exact service versions and layer references yields production-ready scaffolding immediately |
| 2026-03-27 | GitHub Copilot CLI (Claude Sonnet 4.6) | Read project brief PDF (Sections 1–4) + analysis.txt, perform full codebase exploration, then produce a complete implementation plan covering all 46 mandatory requirements (PM/FE/BE/DO/FS), domain model, data flow architecture, 6 implementation phases, infrastructure decisions, 66 tracked todos with dependencies, and resolution of key open questions (cloud provider, team size, log agent, infra approach) | ⭐⭐⭐⭐⭐ Excellent | ~6 hours | Feeding the AI both the requirements doc AND the existing codebase state in a single prompt produces a plan that is grounded in reality rather than hypothetical — it spotted missing packages, empty stubs, and infrastructure gaps without being told. Asking clarifying questions mid-plan (team size, cloud provider) before writing prevented a lot of rework. |
| 2026-03-28 | GitHub Copilot CLI (Claude Sonnet 4.6) | Generate a complete Sprint 1 Trello-ready backlog for a 2-week sprint (PM Foundation + Frontend MVP start): 4 epic definition cards, 5 PM deliverable task cards, 10 frontend user stories — each with full user story text (As a / I want / So that), detailed acceptance criteria checklists, story point estimates (Fibonacci), MoSCoW priority, assignee role, and epic reference. Also generated Trello board structure, label system, custom fields, sprint capacity table, and Definition of Done. | ⭐⭐⭐⭐⭐ Excellent | ~4 hours | Providing the AI with the project brief requirements (PM-1 through FE-12), the implementation plan, and the specific output format (Trello card structure) in a single prompt produced directly usable cards with zero reformatting needed. Asking for acceptance criteria as checklists rather than prose is the key detail — prose ACs are vague, checklist ACs are testable and directly pasteable into Trello. Story point estimates were reasonable and consistent with complexity. |
| 2026-03-30 | GitHub Copilot CLI (Claude Sonnet 4.6) | Generate a detailed Figma Make prompt to produce high-fidelity UI mockups for all 5 key StackSift screens: Login, Dashboard Overview, Log Explorer, Incident Detail with AI Analysis panel, and Alert Rule Builder wizard. Prompt includes full design system spec (colour palette, severity token system, typography, border radius, icon style), persistent app shell layout, and per-screen content descriptions with realistic data | ⭐⭐⭐⭐⭐ Excellent | ~3 hours | The quality of Figma Make output is entirely determined by the quality of the input prompt. Specifying exact hex codes, font names, component dimensions, realistic data values, and the emotional tone ("dense but not cluttered — developer tool aesthetic") dramatically improves fidelity. Identifying the hero element ("the AI Analysis card is the product's centrepiece — make it visually distinct") ensures the AI prioritises what matters most visually. |
| 2026-04-01 | Claude (Claude Sonnet 4.6) | Built M1-product-definition.md across two sessions: first generating the full document (problem statement, personas, JTBD, competitive analysis, hypothesis framing) from the Founder's Brief PDF and EPIC-01 checklist, then refining it using screenshots from the brief to add correct pricing ($0/$19/$79 tiers), Access Levels, Multi-Tenancy, MoSCoW prioritization, and MVP scope by discipline (Frontend/Backend/DevOps). | ⭐⭐⭐⭐⭐ Excellent | ~5 hours | Providing screenshots of exact sections you want replicated eliminates interpretation errors; giving MoSCoW constraints verbatim ("if your Must Have list exceeds 5–7 items, cut again") forces the AI to self-enforce ruthlessness rather than pad the list. Iterating across sessions works better than one long prompt — the second session was more precise because it had a concrete baseline to react to. |
| 2026-04-01 | GitHub Copilot CLI (Claude Sonnet 4.6) | Asked for the recommended Git branching strategy for a 3-person capstone team over 12 weeks — covering permanent branches (main + develop), short-lived feature branch naming conventions (type/issue-number-description), milestone branches, the full PR flow, practical team rules (one branch per card, max 3-day lifetime, squash-merge), and GitHub branch protection rule configuration | ⭐⭐⭐⭐⭐ Excellent | ~1 hour | AI-generated branching strategies are most useful when you provide team size, project duration, and existing conventions (our .cursorrules already had Conventional Commits) — it tailored the output to those constraints rather than giving a generic answer. The cheatsheet format (copy-paste bash commands) is the most actionable output to ask for. |
| 2026-04-01 | GitHub Copilot CLI (Claude Sonnet 4.6) | Asked what remained on PM-04 (Trello board setup card), then requested clarification on what "Sprint 2–6 backlog stories" means and exactly what to enter into Trello's BACKLOG column — received a full table of 48 backlog cards grouped by sprint with title, label, and MoSCoW priority ready to paste into Trello. Also clarified Trello board structure: single BACKLOG column for all future work, one active sprint column renamed each sprint, sprint labels on cards for filtering | ⭐⭐⭐⭐⭐ Excellent | ~2 hours | Using the AI as a project management assistant to decompose a checklist item ("what does this acceptance criterion actually mean?") saves significant time when requirements are ambiguous. The AI surfaced the Trello structure question unprompted and corrected the misconception about creating separate sprint columns before it caused rework. |
| 2026-04-01 | GitHub Copilot CLI (Claude Sonnet 4.6) | Implemented FE-01: installed all 15 production packages (@tanstack/react-query v5, @tanstack/react-table, @tanstack/react-virtual, zustand, react-hook-form, zod, @hookform/resolvers, axios, framer-motion, class-variance-authority, clsx, tailwind-merge, lucide-react, @microsoft/signalr, posthog-js) and 11 dev packages (jest, testing-library stack, ts-jest, playwright, @typescript-eslint plugin+parser, react-query-devtools). Configured eslint.config.mjs with no-explicit-any:error, next.config.ts with reactStrictMode, jest.config.ts with ts-jest + jsdom, playwright.config.ts, .env.local.example. Both pnpm build and pnpm lint pass clean. | ⭐⭐⭐⭐⭐ Excellent | ~2 hours | Letting the AI handle the full dependency installation and config file generation in one pass (rather than installing packages manually and writing configs separately) eliminates version conflicts and copy-paste errors. The AI caught that pnpm wasn't installed globally on the machine, installed it, then continued — zero manual intervention needed. It also caught the Jest config typo (setupFilesAfterFramework vs setupFilesAfterEnv) before it would have caused a silent test failure. |
| 2026-04-03 | Gemini 3 Flash,"Implemented EPIC-03: Core Design System. Configured Tailwind v4 with a ""Dark Mode First"" architecture. Defined design tokens for 5 severity levels (Critical to Info) using a dual-variable strategy (solid + alpha backgrounds). Created a /dev/colours verification page with a React state toggle to validate theme switching. Authored a Jest unit test suite using data-testid to verify DOM class manipulation and typography rendering. Resolved environment-specific ts-node dependency issues for the test runner." | ⭐⭐⭐⭐⭐ Excellent | ~4 hours | "Tailwind v4 processes theme variables differently than v3; mapping internal @theme values to raw CSS variables in :root is essential for reliable access via inline React style props. ""Dark mode first"" implementation requires the :root to hold dark values by default to prevent ""flashing"" during SSR. Using data-testid for theme-wrapper selection is significantly more robust for testing global state changes than standard DOM traversal." |
| 2026-04-02 | GitHub Copilot CLI (Claude Opus 4.6) | Implemented FE-03: built 9 primitive UI components (Button, Badge, Input, Textarea, Card, Skeleton, Spinner, Separator, Tooltip) + cn() utility + barrel export. Used CVA for variant management, Framer Motion for Skeleton pulse animation, React 19 useId for SSR-safe IDs, React.cloneElement for Tooltip's accessible aria-describedby injection. Created a globals.css stub with Tailwind v4 @theme severity colour tokens and @variant dark class-based dark mode as a workaround for FE-02 not yet being merged. Wrote 64 unit tests across 9 test suites using RTL + jest. Fixed 3 lint errors (empty interfaces, ref-in-render, React 19 ReactElement props: unknown), fixed Framer Motion / React HTMLAttributes type conflict, installed ts-node for jest.config.ts parsing. Final state: pnpm lint + pnpm build + 64/64 tests all clean. | ⭐⭐⭐⭐⭐ Excellent | ~5 hours | Working around an unfinished dependency (FE-02) by defining a token contract (CSS variable names agreed with the team) in a stub file means parallel development is unblocked. The AI identified every lint/TS error before committing — no broken commits. React 19's stricter ReactElement typing (props: unknown instead of props: any) is a recurring source of type errors when using cloneElement; always type the generic parameter explicitly. |
| 2026-04-02 | GitHub Copilot CLI (Claude Sonnet 4.6) | Implemented FE-04: built 5 data display components — Dropdown (ARIA menu button pattern, full keyboard navigation with ArrowUp/Down/Escape, click-outside close, disabled items), EmptyState (icon/title/description/CTA slots), Modal (manual focus trap cycling Tab within dialog, Escape+backdrop close, Framer Motion scale+fade with AnimatePresence exit animation, scroll lock), Toast+ToastContainer (4 variants, CSS progress bar shrink animation, auto-dismiss, Zustand-powered via useToastStore with devtools middleware, aria-live=assertive for errors), DataTable (@tanstack/react-table with sort/column-visibility/row-selection + @tanstack/react-virtual with paddingTop/paddingBottom spacers for 10k-row virtualisation, sticky header, cursor pagination). Added useToastStore.ts Zustand hook. Updated barrel export. 103/103 tests passing across 14 suites. | ⭐⭐⭐⭐⭐ Excellent | ~6 hours | Mocking @tanstack/react-virtual in jsdom is essential — without it the virtualizer returns 0 virtual items (no scroll container) and all row-rendering tests fail silently. The Dropdown wrapping the trigger in a className="contents" button causes duplicate ARIA role matches in RTL queries — use getAllByRole when the Dropdown is composed inside another component. The CSS progress bar shrink trick (requestAnimationFrame → width: 0%) is far cheaper than a JS setInterval updating state every frame. |
| 2026-04-03 | GitHub Copilot CLI (Claude Sonnet 4.6) | Post-FE-03/FE-04 branch review: colleague merged FE-02 (Tailwind theme) onto the FE-03 branch. Ran full test suite after conflict resolution. Found and fixed 3 console.error issues: (1) Tooltip.test.tsx — raw DOM .focus()/.blur() calls outside act() causing React 19 state-update warnings; fixed by wrapping in await act(async () => {}). (2) Dropdown.tsx — <button className="contents"> wrapper nesting inside a <button> trigger (invalid HTML, hydration error); fixed by switching to React.cloneElement to inject ARIA props directly onto the trigger element, removing the wrapper entirely. (3) Toast.test.tsx — Framer Motion mock spreading layout/initial/animate/exit/transition props to real DOM <div>; fixed by destructuring with key-alias pattern (layout: _layout etc.) so they never reach the DOM. Final: 105/105 tests, pnpm lint 0 errors, zero console.error warnings. | ⭐⭐⭐⭐⭐ Excellent | ~1.5 hours | When a colleague merges their branch into yours, always re-run the full test suite with console output visible — warnings that were acceptable before may become errors in combination with new code. The React.cloneElement ARIA injection pattern (no wrapper element) is strictly better than className="contents" wrappers for interactive triggers: avoids invalid HTML nesting, avoids duplicate ARIA role matches in RTL, and passes the React Compiler's react-hooks/refs lint rule. |
| 2026-04-03 | GitHub Copilot CLI (Claude Sonnet 4.6 → Opus 4.6) | Implemented FE-06: built the complete client-side state management layer — 16 new files, 1 modified. Phase 1: domain types (types/domain.ts — 9 entity interfaces: Organization, Project, LogSource, LogEntry, AlertRule, Alert, Incident, AiAnalysis, User + 7 type-union enums: LogLevel, AlertSeverity, IncidentStatus, UserRole, LogSourceType, AlertRuleCondition, AiAnalysisStatus) + API response types (types/api.ts — ApiResponse<T>, PaginatedResponse<T>, CursorPaginatedResponse<T>, ApiError envelopes + Zod runtime validation schemas for every entity). Phase 2: Axios instance (lib/api-client.ts — NEXT_PUBLIC_API_URL base, auth interceptor reads useAuthStore.getState().token, error interceptor fires toast on 401/403/4xx/5xx, X-Correlation-ID UUID header per request for end-to-end tracing), QueryClient factory (lib/query-client.ts — makeQueryClient() + createQueryClientWithErrorHandler() with QueryCache/MutationCache global error handlers → toast; 30s staleTime, 5min gcTime, retry 3 exponential backoff), query key factory (lib/query-keys.ts — type-safe object-based keys: queryKeys.projects.all/list/detail(id), logs.list(filters), incidents.*, alerts.* for precise cache invalidation). Phase 3: Zustand stores — useAuthStore (user/token/isAuthenticated/isLoading stub for BE-03 Keycloak; devtools middleware) + useUIStore (sidebarCollapsed, mobileDrawerOpen, activeProjectId, theme light/dark/system; devtools + persist middleware to localStorage, partialize excludes mobileDrawerOpen). Phase 4: mock data (lib/mock-data.ts — 2 orgs, 3 users at owner/admin/member roles, 3 projects, 25 log entries across all 6 LogLevels and 3 services, 5 alerts at all 4 severities, 4 incidents at all 4 statuses) + TanStack Query v5 hooks: useProjects/useProject(id), useLogEntries(filters, limit) with cursor pagination + full filtering, useIncidents/useIncident(id) + useMutateIncidentStatus() with full optimistic update/rollback/invalidation pattern, useAlerts/useAlert(id) + useCreateAlert() mutation. All hooks use 300ms simulated delay so Skeleton loading states render in development. Phase 5: updated Providers.tsx — replaced stub with QueryClientProvider using useState lazy initializer (not useRef — avoids react-hooks/refs lint rule from React Compiler), added ReactQueryDevtools (dev only, tree-shakes in prod). Phase 6: 44 new tests — useUIStore (15: initial state, toggle/set sidebar, theme cycling, activeProject set/clear, mobile drawer), useAuthStore (13: null initial state, setUser flips isAuthenticated, setToken alone does not, logout/reset clear all), use-projects (8: loading→data, typed shape, key prefix, detail, not-found error, disabled when empty), use-logs (8: paginated envelope, projectId/level/search filters, newest-first sort, limit+hasMore, detail). Fixed: useMutateIncidentStatus needed explicit TContext generic for onError rollback typing. Final: pnpm lint ✅ 0 errors, pnpm build ✅ 14 routes, pnpm test ✅ 149/149. | ⭐⭐⭐⭐⭐ Excellent | ~4 hours | The React Compiler's react-hooks/refs rule forbids reading ref.current during render — the common QueryClient-in-a-ref pattern (`if (!ref.current) ref.current = new QueryClient()`) fails lint. The fix is useState with a lazy initializer: `const [qc] = useState(() => makeQueryClient())` — identical behaviour (created once, stable across re-renders), no lint violation. When typing TanStack Query v5 mutations with optimistic updates, you MUST provide the 4th generic argument `TContext` to `useMutation<TData, TError, TVars, TContext>` — otherwise the `context` parameter in `onError` is typed as `{}` and `.previous` doesn't exist. Object-based query key factories (`queryKeys.projects.all` → `['projects']`) are strictly better than raw string arrays: prefix-matching invalidation, zero typo risk, TypeScript catches shape mismatches at compile time. Defining Zod schemas alongside TypeScript interfaces (not instead of) catches backend contract drift at runtime while keeping compile-time type safety — the two serve different purposes. |
| 2026-04-03 | GitHub Copilot CLI (Claude Sonnet 4.6) | Implemented FE-05: scaffolded the full Next.js 16 App Router route structure. Created 27 files across 2 route groups: (auth) — centred layout, /login stub (disabled Google SSO button), /callback stub (spinner). (dashboard) — minimal hardcoded sidebar using <Link> (no useUIStore to avoid FE-06 merge conflict), /page.tsx overview, /logs, /incidents, /incidents/[id], /alerts, /alerts/new, /projects, /projects/new, /projects/[id], /settings — each with page.tsx + loading.tsx (using <Skeleton> from FE-03). Root: Providers.tsx client boundary wrapper, updated layout.tsx with <Providers> + <ToastContainer>, custom not-found.tsx, root error.tsx. Proxy stub (src/proxy.ts) with dashboard matcher. Deleted boilerplate page.tsx. All pages are Server Components; dynamic routes use async params (Next.js 16 breaking change). Investigated Next.js 16 docs: params/searchParams are now Promises, middleware.ts renamed to proxy.ts, error.tsx uses unstable_retry not reset(). Build: 14 routes compiled (○ static, ƒ dynamic), proxy registered. Lint: 0 errors. Tests: 105/105 passing. | ⭐⭐⭐⭐⭐ Excellent | ~2.5 hours | Reading the actual Next.js 16 docs before writing any routing code prevented 3 silent breaking-change bugs (async params, proxy.ts rename, unstable_retry). The workaround for parallel-branch conflicts (use only <Link> hardcoded nav, zero store imports in layout) follows the same token-contract pattern used in FE-02/FE-03 — agree on the interface, stub the implementation, merge cleanly later. Deleting the root page.tsx is the correct move when a route group owns the / URL; leaving both causes a build-time route conflict that is non-obvious to diagnose. |
| 2026-04-04 | GitHub Copilot CLI (Claude Sonnet 4.6) | Implemented US-01: complete Google SSO authentication via BFF (Backend-for-Frontend) pattern. 18 new files, 4 modified, 174/174 tests passing. Phase 1 — Auth utilities (lib/auth/): config.ts centralises all Keycloak URLs, client_id, redirect_uri, cookie names, and NEXT_PUBLIC_AUTH_MOCK flag; pkce.ts uses Web Crypto API (crypto.subtle.digest) to generate cryptographically random state/nonce/code_verifier/code_challenge; mock.ts generates three-part JWT tokens with correct Keycloak claim structure (sub, email, name, organization_id, stacksift_role, realm_access.roles) for local dev without Docker; session.ts manages the stacksift_session HTTP-only cookie — createSessionCookie (httpOnly, secure in prod, sameSite=lax, maxAge=1d), readSessionCookie (JSON parse + validate), clearSessionCookie, getSessionUser (JWT payload decode), isSessionExpired (30s buffer), redirectWithClearedSession. Phase 2 — Route Handlers: /api/auth/login generates PKCE state, stores {codeVerifier, nonce, next} in short-lived cookie (stacksift_pkce_{state}=, Max-Age=600), validates ?next= against open redirect (rejects // and absolute URLs), redirects to Keycloak authorize or mock callback; /api/auth/callback validates state against PKCE cookie, exchanges code for tokens (mock: generateMockTokens(), real: POST to Keycloak /token with code_verifier), sets stacksift_session cookie, clears PKCE cookie (single-use), redirects to / or saved ?next= URL; /api/auth/logout clears session cookie, redirects to Keycloak /logout?id_token_hint= (mock: just clears and goes to /login); /api/auth/me reads session cookie, calls isSessionExpired(), calls getSessionUser(), returns user JSON or 401. Phase 3 — Client auth: useSession hook (TanStack Query, staleTime:Infinity, no retry on 401, cookie→JSON→Zustand bridge) + AuthGuard component (client wrapper in dashboard layout: loading overlay → redirect to /login?next=pathname → render children). Phase 4 — Page updates: login/page.tsx converted from disabled button stub to enabled <a href="/api/auth/login"> with useSession redirect-if-authenticated and ?error= toast handling; callback/page.tsx shows error state + auto-redirects to /login after 2s; dashboard layout.tsx wraps children in <AuthGuard>. Phase 5: .env.local.example adds NEXT_PUBLIC_AUTH_MOCK=true + NEXT_PUBLIC_APP_URL; useAuthStore gains setLoading action. Phase 6 — Tests: login.test.ts (node env, 9 tests — mock redirect, PKCE cookie contents, next= passthrough, open redirect rejection, real mode Keycloak URL params); callback.test.ts (node env, 9 tests — error cases, invalid_state, successful redirect, session cookie set, PKCE cookie cleared); useSession.test.tsx (12 tests — loading state, authenticated hydration, store hydration, token sentinel, unauthenticated reset, no retry on 401, return shape). Fixed: NextRequest tests require @jest-environment node docblock (jsdom doesn't expose Request global); cookies must be passed in NextRequest constructor init not set on headers after construction. Final: pnpm lint ✅ 0 errors, pnpm build ✅ 18 routes (4 new ƒ dynamic API handlers), pnpm test ✅ 174/174. | ⭐⭐⭐⭐⭐ Excellent | ~5 hours | The BFF cookie pattern (HTTP-only session cookie + /api/auth/me bridge endpoint) is strictly better than storing JWTs in localStorage or client-accessible cookies — no XSS token theft possible because JavaScript can never read the cookie. Testing Next.js Route Handlers in Jest requires @jest-environment node docblock (not jsdom) because NextRequest extends the Web API Request which isn't available in JSDOM. Passing cookies to NextRequest must happen in the constructor options (new NextRequest(url, { headers: { cookie: '...' } })) — you cannot set them after construction because the internal cookie jar is read-only. The mock auth mode pattern (NEXT_PUBLIC_AUTH_MOCK=true flag generates realistic JWTs without real Keycloak) is a powerful technique for parallel development: the full auth flow, AuthGuard redirect logic, and session cookie shape are all testable before Docker infrastructure exists. |
| 2026-04-04 | GitHub Copilot CLI (Claude Sonnet 4.6) | Implemented US-02: sign-out capability with UserMenu component in the dashboard sidebar. 4 new files, 1 modified, 189/189 tests passing. Phase 1 — useSignOut hook (hooks/useSignOut.ts): encapsulates the three-step sign-out sequence — (1) useAuthStore.logout() fires synchronously to clear Zustand user/token/isAuthenticated before any navigation starts, preventing a race condition where AuthGuard might re-render with stale state and trigger a competing redirect; (2) queryClient.clear() wipes all TanStack Query cache entries including ['auth','me'], projects, logs, and incidents so no sensitive data from the previous session leaks to the next user or to the same user's next login; (3) window.location.href = '/api/auth/logout' triggers a full browser navigation (not router.push) because the /api/auth/logout Route Handler terminates the session via a Set-Cookie: Max-Age=0 header — router.push would intercept the request as a client-side fetch and discard the cookie-clearing header. isLoading guard prevents double-click re-invocation during the ~100ms between click and navigation. Phase 2 — UserMenu component (components/auth/UserMenu.tsx): client component at the sidebar bottom (mt-auto within the flex-col aside). Displays avatar initials (first letter of displayName, bg-blue-600 circular badge), full display name, and email address (truncated with Tailwind truncate). Reuses the FE-04 Dropdown component, inheriting its full ARIA Menu Button pattern (keyboard navigation, aria-haspopup, aria-expanded, aria-controls, role=menu, role=menuitem) — no new accessibility code needed. Single "Sign out" item with LogOut icon from lucide-react. Item is disabled and shows "Signing out…" label when isLoading=true. Returns null when user is null (safe during AuthGuard loading overlay). Phase 3 — Dashboard layout update: added UserMenu import and rendered it inside <aside> after the nav links. Layout stays as a Server Component — importing a client component (UserMenu) into a server layout is valid in Next.js App Router. Phase 4 — Tests: useSignOut.test.tsx (6 tests: isLoading starts false, signOut function exists, logout() called, queryClient cleared, window.location.href set to /api/auth/logout, isLoading becomes true, double-click guard). UserMenu.test.tsx (8 tests: displayName rendered, email rendered, avatar initials correct, different user initials, accessible trigger label, dropdown shows Sign out on click, clicking Sign out calls signOut(), null when no user). Mocking strategy: window.location re-defined as writable (jsdom makes it read-only); useSession mocked to avoid network calls; Framer Motion motion.div mocked to strip animation props. Final: pnpm lint ✅ 0 errors, pnpm build ✅ 18 routes, pnpm test ✅ 189/189. | ⭐⭐⭐⭐⭐ Excellent | ~1 hour | The order of operations in a sign-out sequence matters: clear client state BEFORE navigating, not after. If you navigate first, AuthGuard sees isAuthenticated=false (because the cookie is cleared server-side) and starts a redirect simultaneously with the logout redirect — two competing redirects cause a flash or loop. Clearing synchronously first, then navigating, means AuthGuard never gets a chance to re-render with stale state. window.location.href is the correct way to invoke any Route Handler that sends Set-Cookie or Location response headers — Next.js router.push treats Route Handlers as API endpoints and returns the response body, discarding navigation-related headers. Reusing an existing component (Dropdown) for the UserMenu saved building an entire accessible menu from scratch — but it only works if the component was designed with a generic enough API (trigger: ReactNode, items: DropdownItem[]) from the start. |
| 2026-04-05 | GitHub Copilot CLI (Claude Sonnet 4.6) | Implemented US-03: onboarding flow — Create Organisation form. 8 new files, 6 modified, 219/219 tests passing. Phase 1 — Type & Auth changes: changed User.organizationId from `string` to `string | null` in types/domain.ts to explicitly model the pre-onboarding state (authenticated but no org yet); updated extractUserFromToken() in mock.ts to return null (not '') when the JWT has no organization_id claim — critical because empty string would bypass OnboardingGuard's null check; added generateMockTokensForUser(user: User) to mock.ts that generates JWT tokens for any user object (used by the create-org Route Handler to inject the new organization_id claim into the session after org creation); added replaceSessionCookie(updatedUser) to session.ts as a thin wrapper calling generateMockTokensForUser + createSessionCookie; added NEXT_PUBLIC_AUTH_MOCK_NEW_USER env var checked in callback/route.ts — when true, mock login returns a user with organizationId: null to trigger the onboarding flow. Phase 2 — Zod schema + Route Handler: created lib/schemas/organisation.ts with createOrganisationSchema — name: z.string().min(2).max(50).regex(/^[a-zA-Z0-9][a-zA-Z0-9 -]*$/) — first character must be alphanumeric, remaining allow spaces and hyphens; this schema is the single source of truth shared by both React Hook Form (via zodResolver) and the Route Handler (via safeParse); created api/onboarding/create-org/route.ts — POST handler that (1) reads + validates session cookie → 401 if missing, (2) parses request body with Zod → 400 validation_failed if invalid, (3) calls crypto.randomUUID() to generate the org ID, (4) converts name to slug (lowercase, spaces→hyphens, collapse multiple hyphens, strip trailing), (5) calls generateMockTokensForUser with the updated user and sets the new session cookie on the response so /api/auth/me immediately returns organizationId non-null after the mutation succeeds. Phase 3 — Mutation hook + Guard: created hooks/useCreateOrganisation.ts — useMutation wrapping POST /api/onboarding/create-org; onSuccess AWAITS queryClient.invalidateQueries(['auth', 'me']) before calling router.push('/') — this is critical to prevent an infinite onboarding redirect loop (if you navigate before the session refetch completes, OnboardingGuard sees null org and sends you back); shows "Welcome to StackSift!" success toast or error toast on failure; created components/providers/OnboardingGuard.tsx — client component nested inside AuthGuard in the dashboard layout (AuthGuard > OnboardingGuard > children) — checks user.organizationId === null and redirects to /onboarding, returns null during redirect to prevent content flash. Phase 4 — Onboarding page: created (auth)/onboarding/page.tsx — lives in the (auth) route group (inherits centred no-sidebar layout); uses useForm with zodResolver(createOrganisationSchema) and mode:'onChange' for live keystroke validation; three-way redirect guard (loading → spinner, no user → /login, has org → /); single <Input> with errorMessage wired to errors.name?.message; <Button loading={isPending} disabled={!isValid||isPending}> using the existing Button component's loading prop (not isLoading — caught by TypeScript build); dashboard layout updated to wrap children in <OnboardingGuard> inside <AuthGuard>. Phase 5 — Tests (30 new): OnboardingGuard.test.tsx (4 tests: loading renders children, has-org renders children, no-org redirects to /onboarding, no-org returns null); create-org.test.ts (node env, 13 tests: 401 no cookie, 400 missing name, 400 too short, 400 too long, 400 invalid chars, 400 leading space, 201 success object shape, session cookie set on response, accepts spaces+hyphens; nameToSlug 5 tests: lowercase, spaces→hyphens, multiple spaces→single hyphen, trailing hyphens stripped, existing hyphens preserved); onboarding.test.tsx (13 tests: spinner while loading, no form while loading, redirect to /login when unauthenticated, redirect to / when has org, input renders, button renders, first-name greeting, button disabled when empty, validation error too short, validation error invalid chars, button enabled when valid, calls createOrganisation on submit). Fixed: Button prop is loading not isLoading (TypeScript build caught this); form submission test needed waitFor to confirm button was enabled before clicking (RHF mode:'onChange' is async). Final: pnpm lint ✅ 0 errors, pnpm build ✅ 19 routes (+/onboarding +/api/onboarding/create-org), pnpm test ✅ 219/219. | ⭐⭐⭐⭐⭐ Excellent | ~3 hours | React Hook Form + Zod via zodResolver is the cleanest form validation pattern in the React ecosystem — declare the schema once, get TypeScript types, runtime validation, and live error messages for free with zero boilerplate. The session cookie update pattern (regenerate JWT with new claim → Set-Cookie on the API response → TanStack Query invalidation refetches /api/auth/me → Zustand updates automatically) is the correct way to propagate state changes through the BFF pattern without re-login. The infinite redirect loop pitfall: if a mutation's onSuccess navigates before the cache invalidation refetch completes, the guard re-evaluates against stale data and sends the user back — always await queryClient.invalidateQueries() before navigating. |
| 2026-04-05 | GitHub Copilot CLI (Claude Sonnet 4.6) | Implemented US-04: Dashboard empty state. 2 new files, 4 modified, 240/240 tests passing. Transformed (dashboard)/page.tsx from a static Server Component stub into a 'use client' data-driven component. The page calls useProjects(), useAlerts(), useIncidents(), and useLogEntries() TanStack Query hooks. When projects.data.length === 0 (post-onboarding, user has an org but no projects yet), renders <EmptyState> with FolderOpen icon, "No projects yet" title, description, and "Create Project" CTA navigating to /projects/new. When projects exist, renders 3 metric cards (Active Alerts, Total Logs Today, Open Incidents) with real computed counts. All cards show '—' when no projects or while loading. metadata moved from page.tsx to layout.tsx (title.default + template pattern). (dashboard)/error.tsx rewritten with <Card role="alert"> + <Button>Reload</Button>. loading.tsx updated to 3 skeleton cards. Added 16 page tests + 6 error tests. CursorPaginatedResponse.data (not .items) caught and fixed during build. pnpm lint ✅, pnpm build ✅ 20 routes, pnpm test ✅ 240/240. | ⭐⭐⭐⭐⭐ Excellent | ~1 hour | Converting a Server Component to 'use client' silently removes the metadata export — TypeScript does not warn, but the browser tab title stops updating. Always move metadata to the nearest Server Component parent (the layout). The title.template pattern ('%s — StackSift') lets child pages override the suffix-free title while the layout handles the brand. Testing empty vs populated state: mock the hook return value in tests (never modify mock-data.ts) — this keeps tests isolated and the production data intact. |

